{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "description": "URL shortener with click analytics (issue #1)",
  "main": "dist/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "restart": "node scripts/restart.mjs",
    "stop": "node scripts/restart.mjs --stop-only"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
